import './Header.css';
import PropTypes from 'prop-types';

const Header = () => {
  return (
    <header className="header">
      <h1 className="header-text">Joke</h1>
    </header>
  );
};

Header.propTypes = {};

export default Header;
